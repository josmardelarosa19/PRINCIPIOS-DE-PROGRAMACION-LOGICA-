// Imprimir en consola si existe mas de 20 productos en stock

const numerodeproductos = 20
const mensaje = "¿Hay mas de 20 productos en stock?"

let respuesta = numerodeproductos > 20 ? "Sí, hay más de 20 productos en stock." : "No, no hay más de 20 productos en stock."


console.log(mensaje + " " + respuesta)
